/**
 * Whenever creates function so function name starting letter should be in uppercase
 * In the function always remember use multiple tags in single main division. Because function return only 1 element. 
 */
function Chai() {
  return (
    <div>
      <h1>Hello from Chai file</h1>
    </div>
  )
}

export default Chai